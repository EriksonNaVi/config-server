server:
  port: 8081
spring:
  security:
    user:
      name: maven
      password: m4ven
  cloud:
    config:
      server:
        git:
          uri: https://github.com/EriksonNaVi/config-repository.git
          search-paths: /
          default-label: "master"